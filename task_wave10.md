# Task Wave 10

- [x] Create a global Cmd+K command menu component (`components/layout/command-menu.jsx`). Use `framer-motion` to animate the modal opening. It should list links to the main pages (Home, Components, Pricing, Themes, API, Authors). Import and render it inside `components/layout/app-header.jsx` (add a search button next to the API link that triggers it).
- [x] Add slow pulse animations to background gradient blobs on secondary pages.
